{
  "name": "portfolio",
  "version": "0.1.0",
  "description": "The portfolio front-door — a landing that aggregates the apps, runnable offline.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "sh tools/build.sh"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.6.3"
  }
}
